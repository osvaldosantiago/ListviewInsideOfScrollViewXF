<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		x:Class="ListviewInsideOfScrollViewXF.CustomViewCell">
	<ContentView.Content>
<!--		<ScrollView Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">-->
			<StackLayout Orientation="Horizontal">
				<Label Text="{Binding Name}" WidthRequest="100"></Label>
				<Label Text="{Binding LastName}" WidthRequest="100"></Label>
				<Label Text="{Binding Age}" WidthRequest="100"></Label>
				<Label Text="{Binding School}" WidthRequest="100"></Label>
				<Label Text="{Binding Degree}" WidthRequest="100"></Label>
			</StackLayout>
<!--			</ScrollView>-->
	</ContentView.Content>
</ContentView>
